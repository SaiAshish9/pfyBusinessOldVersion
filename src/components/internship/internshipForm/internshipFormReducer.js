// const internshipFormReducer = (state, action) => {
//   switch (action.type) {
//     case "ADD_FORM_DATA":
//       return {...state, action.notes};

//     //   case "ADD_NOTES":
//     //     return [...state, { title: action.title, body: action.body }];

//     //   case "REMOVE_NOTE":
//     //     return state.filter(note => note.title !== action.title);

//     default:
//       return state;
//   }
// };

// export { internshipFormReducer as default };
